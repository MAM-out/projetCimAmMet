{% extends 'base.html.twig' %}

{% block body %}


<div class="container">
  <div class="row ">
  <div class="mx-auto">
  <div class="card m-1 " >
    <div class="card-body">

       <div class="row  text-center">
        <div class="col">
          <h5 >BIENVENUE DANS L'INTERFACE DE GESTION </h5>    
        </div>
      </div>

      <form action="{{ path('security_login')}}" method="post" >

        <div class="row " >
          <div class="col text-center">
            <div class="form-group "> 
              <input type="text" class="form-control text-center" required placeholder="Domaine | Nom d'utilisateur" name="_username"> 
            </div> 
            <div class="form-group "> 
              <input type="text" class="form-control text-center" required placeholder="Email" name="_email" >
            </div> 

            <div class="form-group "> 
              <input type="password" class="form-control text-center" required placeholder="Mot de passe" name="_password"> 
            </div> 
              <div class="form-group"><button type="submit" class="btn btn bg-primary">CONNEXION</button></div>
          </div>     
        </div>

      
      </form> 

        </div>  
    </div>
    </div>
</div>
 

{#
        {{ form_start(form) }}
        {{ form_row(form.username, {'label':'Nom Session', 'attr': {'placeholder' : 'Nom Session'}}) }}
        {{ form_row(form.roles, {'label':'Roles ', 'attr': {'placeholder' : 'Roles'}}) }}
        {{ form_row(form.email, {'label':'Email ', 'attr': {'placeholder' : 'Email'}}) }}
        {{ form_row(form.password, {'label':'MDP ', 'attr': {'placeholder' : 'Mot de passe'}}) }}

        <button type="submit" class="btn btn-primary">VALIDER</button>
        {{ form_end(form) }}
#}
    </div>
{% endblock %}